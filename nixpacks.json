{
  "providers": ["python"],
  "pkgs": ["ffmpeg", "python311"],
  "phases": {
    "setup": {
      "nixPkgs": ["ffmpeg", "python311"]
    }
  },
  "start": {
    "cmd": "gunicorn --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120 app:app"
  }
}
